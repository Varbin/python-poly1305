language: python

# we only test stable versions
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "pypy"

# command to install dependencies
install: "python setup.py install"

# command to run tests
script: "python poly1305.py"
